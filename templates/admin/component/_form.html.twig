<div class="margin">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="text-center">
            <button class="btn btn-secondary mt-4">{{ button_label|default('Save') }}</button>
        </div>
    {{ form_end(form) }}
</div>
